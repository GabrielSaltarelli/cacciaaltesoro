function doLogin () {
  document.getElement.Byld ("username") .value;
  document. getElementById ("password") .value;
    if (document.getElementById ("username"). value == "" || document.getElementById ("password"). value == "") {
alert ("Username e password sono obbligatori"); 
    }
  }
