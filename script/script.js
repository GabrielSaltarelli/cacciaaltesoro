function doLogin(allert) {
    var usern = document.getElement.ById ("username") .value;
    var passw = document.getElement.ById ("password") .value;
      if(usern == "" || passw == "") {
        alert ("Username e password sono obbligatori"); 
    }
  }
