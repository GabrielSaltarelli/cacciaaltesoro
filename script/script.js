function doLogin(allert) {
    var usern = document.getElement.Byld ("username") .value;
    var passw = document.getElement.Byld ("password") .value;
      if(usern == "" || passw == "") {
        alert ("Username e password sono obbligatori"); 
    }
  }
